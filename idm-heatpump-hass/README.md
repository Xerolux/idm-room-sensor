# Proposed upstream changes

This directory contains a reviewable patch design for `idm_heatpump.set_external_climate`. It is not automatically applied to the upstream repository and has not been executed against a real heat pump.
