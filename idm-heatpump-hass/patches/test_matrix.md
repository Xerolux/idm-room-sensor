# Test matrix

Circuits A–G, 15–30 °C temperature limits, 0–100 % humidity limits, invalid
circuit, missing/read-only registers, non-finite values, optional humidity and
no partial write on preflight validation failure.
