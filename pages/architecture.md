# Architecture

The preferred path is software-first via writable GLT registers, with the analog bridge retained as a compatibility and fallback option. Sensor fusion must select the smallest dew-point margin, not merely the largest humidity.
