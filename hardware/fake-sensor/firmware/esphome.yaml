# Hardware-local entry point. The canonical configuration lives in firmware/.
substitutions:
  idm_bridge_components: ../../../firmware/components

packages:
  idm_fake_sensor: !include ../../../firmware/fake-sensor-esphome.yaml
